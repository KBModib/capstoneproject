module.exports = {
    // ... (other existing configuration properties)
    moduleNameMapper: {
        '\\.(css|less)$': 'identity-obj-proxy',
    },
    // transformIgnorePatterns: [
    //   'node_modules/(?!(react-datepicker)/)',
    // ],
    // ... (other existing configuration properties)
  };
  